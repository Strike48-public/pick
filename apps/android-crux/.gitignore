# Build outputs
.gradle/
.gradle-nix/
build/
app/build/
app/.cxx/

# Local SDK config (nix injects SDK via env; regenerated at build time)
local.properties

# Prebuilt native lib — regenerate via `just crux-ffi-android-x86_64`
app/src/main/jniLibs/**/libpick_crux_ffi.so

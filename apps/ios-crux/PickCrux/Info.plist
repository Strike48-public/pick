<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Sentry native crash/hang capture config, substituted from the SENTRY_DSN
	     / SENTRY_ENV build settings (which come from the build env). Empty DSN =>
	     the native SDK stays uninitialised (see PickCruxApp). -->
	<key>SentryDSN</key>
	<string>$(SENTRY_DSN)</string>
	<key>SentryEnv</key>
	<string>$(SENTRY_ENV)</string>
	<!-- Strike48 PLG host, substituted from the STRIKE48_HOST build setting
	     (build env). Empty/unsubstituted falls back to the dev cluster in code. -->
	<key>Strike48Host</key>
	<string>$(STRIKE48_HOST)</string>
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleTypeRole</key>
			<string>Editor</string>
			<key>CFBundleURLName</key>
			<string>com.strike48.pentest.oauth</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>com.strike48.pentest</string>
			</array>
		</dict>
	</array>
</dict>
</plist>
